

<%- include("partials/Header"); -%>

    <% if(post.title.length !==0) { %>
        
        <h1><%= post.title %></h1>
        <p><%= post.body %></p>
        <% } %>

<%- include("partials/Footer"); -%>